Run $ Rscript a.R to generate latency graph from the data.

packet_latency_sample.txt has data in the following format:
server_time (ns), client_time (ns), latency

nat_map_capacity.txt has data in the following format:
nat_time (ns), len(nat_map)

